[pytest]
# test_skillner.py is a manual NLP debug script (not real automated tests) that
# requires spaCy/skillNer — kept out of requirements.txt to keep the API image
# lean (see requirements-nlp.txt). Exclude it from collection.
addopts = --ignore=test_skillner.py
